# E2E Reports

`npm run test:e2e:*` 执行后的产物主要分布在以下位置：

- `playwright-report/`：Playwright HTML 报告
- `test-results/`：失败时的截图、trace、video、JSON 附件
- `e2e/reports/`：手写说明文档与约定说明

本目录目前用于记录 E2E 报告规范，便于团队统一查看 `lesson picker`、`smoke` 等场景的输出位置。

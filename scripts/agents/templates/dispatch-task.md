你现在是 OpenMAIC 仓库中的 **{{WORKER_LABEL}}**。

请立即执行下面的任务，不要先回复“你想让我做什么”。如果任务已经足够明确，请直接完成；只有在确实无法继续时，才说明缺失信息。

## 当前角色
- 角色：`{{ROLE}}`
- 角色约束：
{{ROLE_GUIDANCE_BLOCK}}

## 任务目标
{{TASK_BLOCK}}

## 工作区
- 仓库根目录：`{{PROJECT_ROOT}}`
- 优先关注路径：
{{PATHS_BLOCK}}
- 可参考上下文文件：
{{CONTEXT_FILES_BLOCK}}

## 验收标准
{{ACCEPTANCE_BLOCK}}

## Token 使用约束
- 只读取完成任务必需的文件。
- 先从给定路径开始，不要上来全仓扫一遍。
- 不要复述整个项目背景，只保留与当前任务有关的信息。
- 输出尽量短，优先给结果、关键文件、验证、风险。

## 输出格式
1. 结果摘要
2. 关键文件 / 证据
3. 风险或后续项（如无可省略）

如果当前角色允许改代码，请直接在工作区内完成最小必要修改；如果当前角色是研究或评审，默认不要改文件。
